# 自主闭环执行约定

对于明确的实现类任务，代理应当采用端到端自主推进方式：只在存在真正阻塞执行的歧义时进行最少必要澄清；需求足够明确后，自主完成需求确认、方案规划、代码实现、测试验证、失败修复和最终汇报。

不要在需求确认、方案规划、代码实现、测试验证、失败修复等阶段之间等待用户逐步确认。只有当下一步操作具有破坏性、不可逆、需要额外权限，或涉及重大产品取舍时，才向用户提问。

执行过程中应保持结果导向：能通过读取代码、运行命令、查看日志、执行测试获得的信息，不要求用户提供；验证未通过时继续定位并修复，直到任务完成、遇到真实阻塞，或用户明确要求停止。

## Maven 发布约定

当用户要求“发布”时，只在当前项目目录执行 `mvn deploy`。不得修改 `pom.xml` 中已有的版本号、不得创建或推送 Git tag、不得自行检查、推断或变更发布目标；Maven 的现有配置决定部署的版本与目标仓库。
