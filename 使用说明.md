# service-support 使用说明

## 1. 项目定位

`service-support` 是一个面向 Spring/Spring Boot 生态的工具库，核心目标是减少业务项目中的重复基础代码。  
从源码看，它主要提供以下能力：

- 编译期常量生成（`E_` 类）
- 基于注解的代理 Bean 自动注册
- 插件管理器（`PluginManager`）
- 变量解析与字段注入（`@InjectVar`）
- Spring MVC 常见参数转换增强（日期、枚举、JSON 字符串）
- RBAC 核心模型与授权匹配逻辑
- 一组常用工具类（签名、JWT、脱敏、分布式锁、事件总线等）

---

## 2. 环境与版本要求

### 2.1 JDK 与构建工具

- `pom.xml` 当前配置为 `source/target/release=11`
- 推荐使用 `JDK 11`
- Maven 3.6+

### 2.2 已验证构建结论

在当前环境验证结果：

- `OpenJDK 11.0.21` 下执行 `clean package`：`BUILD SUCCESS`
- `OpenJDK 25.0.2` 下执行 `clean package`：编译失败（大量 Lombok 生成方法/字段缺失）

JDK 11 构建命令：

```bash
export JAVA_HOME=/Users/lilw/Library/Java/JavaVirtualMachines/corretto-11.0.21/Contents/Home
export PATH="$JAVA_HOME/bin:$PATH"
"/Applications/IntelliJ IDEA.app/Contents/plugins/maven/lib/maven3/bin/mvn" -f pom.xml clean package
```

`clean install` 在受限环境里可能因为无法写入 `~/.m2` 失败，优先用 `clean package` 验证编译与打包。

### 2.3 依赖作用域注意事项

项目中大量依赖是 `provided`，意味着：

- 编译本库需要这些依赖
- 业务应用运行时需要自行提供相关依赖（Spring、Jackson、hutool、fastjson、gson、redisson、jwt 等，按实际功能引入）

---

## 3. 引入方式

## 3.1 JitPack（README 中给出的方式）

```xml
<repositories>
    <repository>
        <id>jitpack.io</id>
        <url>https://www.jitpack.io</url>
    </repository>
</repositories>

<dependency>
    <groupId>com.github.Levin-Li</groupId>
    <artifactId>service-support</artifactId>
    <version>1.2.30-SNAPSHOT</version>
</dependency>
```

## 3.2 本仓库当前坐标（基于 `pom.xml`）

```xml
<dependency>
    <groupId>com.levin.commons</groupId>
    <artifactId>service-support</artifactId>
    <version>1.5.0-SNAPSHOT</version>
</dependency>
```

---

## 4. 构建命令

README 提示每次构建前先 `clean`，建议如下：

```bash
mvn clean install
```

如果你本机没有 `mvn` 命令，可使用 IntelliJ 自带 Maven：

```bash
"/Applications/IntelliJ IDEA.app/Contents/plugins/maven/lib/maven3/bin/mvn" -f pom.xml clean install
```

---

## 5. 自动配置（Spring Boot）

项目通过以下文件暴露自动配置：

- `META-INF/spring.factories`
- `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`

默认自动装配的配置类：

- `PluginConfiguration`
- `SpringContextHolder`
- `VariableResolverConfiguration`
- `DefaultSpringMvcDateFormatterConfiguration`
- `DefaultSpringMvcEnumFormatterConfiguration`
- `DefaultSpringMvcJsonDeserializerConfiguration`

可通过属性禁用：

- `com.levin.commons.plugin.support.PluginConfiguration=disable`
- `com.levin.commons.service.support.VariableResolverConfiguration=disable`
- `com.levin.commons.service.support.DefaultSpringMvcDateFormatterConfiguration=disable`
- `com.levin.commons.service.support.DefaultSpringMvcEnumFormatterConfiguration=disable`
- `com.levin.commons.service.support.DefaultSpringMvcJsonDeserializerConfiguration=disable`

---

## 6. 核心能力使用

## 6.1 编译期生成字段/方法名常量（注解处理器）

处理器入口：

- `JpaEntityClassProcessor`
- `GenNameConstantProcessor`

注册文件：`src/main/processor/META-INF/services/javax.annotation.processing.Processor`

### 用法 A：JPA 实体自动生成 `E_实体名`

对 `@Entity` / `@MappedSuperclass` 类，编译时生成同包接口，如 `E_User`，常量包括：

- 字段名常量
- 表名、实体名
- `F_字段` 形式的占位常量
- 主键字段、唯一字段信息

### 用法 B：`@GenNameConstant`

对类/接口/注解生成 `E_` 常量接口。  
可通过 `genType` 控制生成字段名、方法名或两者。

---

## 6.2 注解扫描 + 代理 Bean 注册

核心注解/类：

- `@ProxyBeanScan`
- `@ProxyBeanScans`
- `@EnableProxyBean`
- `ProxyBeanScanAndRegistrar`
- `ProxyFactoryBean`

支持三种拦截器类型：

- `java.lang.reflect.InvocationHandler`
- `org.aopalliance.intercept.MethodInterceptor`
- `org.springframework.cglib.proxy.MethodInterceptor`

示例：

```java
@Target(ElementType.TYPE)
@Retention(RetentionPolicy.RUNTIME)
public @interface ApiClient {}

@Configuration
@ProxyBeanScans({
    @ProxyBeanScan(
        scanType = ApiClient.class,
        invocationHandlerClass = MyInvocationHandler.class,
        basePackages = "com.demo.client"
    )
})
@EnableProxyBean(registerTypes = {ApiClient.class})
public class ProxyConfig {
}
```

---

## 6.3 插件机制

核心接口：

- `Plugin`
- `PluginManager`
- `PluginConfigurer`

默认实现：

- `PluginManagerImpl`
- `PluginConfiguration`（自动注册默认 `PluginManager`）

行为要点：

- 容器启动完成后，自动收集 `Plugin` Bean 并安装
- 如果 `Plugin` 实现 `PluginManagerAware`，会回调注入管理器
- `sendEvent` 异步发送插件事件（默认尝试使用 `pluginAsyncTaskExecutor`，否则回退 `applicationTaskExecutor`）

---

## 6.4 变量解析与字段注入（`@InjectVar`）

核心接口/类：

- `@InjectVar`
- `VariableResolver`
- `VariableInjector` / `SimpleVariableInjector`
- `VariableResolverManager` / `DefaultVariableResolverManager`

### 表达式前缀

- SpEL：`#!spel:`
- Groovy：`#!groovy:`

### 示例

```java
public class DemoReq {
    @InjectVar("user.id")
    private String userId;

    @InjectVar(value = "#!spel:#root['tenantId']", isRequired = "true")
    private String tenantId;
}
```

调用注入：

```java
variableInjector.injectByMap(req, contextMap);
```

### 可扩展点

- 实现 `VariableResolverConfigurer`，在启动时统一注册你的 `VariableResolver`
- `HttpRequestInfoResolver` 可解析请求相关变量（IP、header、param、url 等）
- 常量名建议复用 `InjectConst`

---

## 6.5 Spring MVC 参数转换增强

自动配置类：

- `DefaultSpringMvcDateFormatterConfiguration`
- `DefaultSpringMvcEnumFormatterConfiguration`
- `DefaultSpringMvcJsonDeserializerConfiguration`

提供能力：

- 多格式日期字符串转 `Date`
- `EnumDesc` 枚举的字符串/数字转换
- 字符串转 `fastjson2 JSONObject` / `fastjson JSONObject` / `gson JsonElement` / `Map`

---

## 6.6 RBAC 核心模型与授权引擎

核心注解/接口/实现：

- `@ResAuthorize`
- `ResConditionAction`
- `RbacBaseService` / `RbacAuthorizeService`
- `AbstractRbacAuthorizeService`
- `RbacUtils`

### 你通常需要实现的接口

- `RbacBaseService`（用户、角色、权限、组织加载）
- `RbacAuthService`（登录态/Token）

### 权限表达式格式

`domain:type:res:action`

支持：

- `*` 通配
- `|` 或匹配
- 角色前缀约定：`R_`

`RbacUtils` 可从控制器 `@ResAuthorize + @Operation` 扫描资源与菜单。

---

## 6.7 其他常用工具

- `SignUtils`：签名生成/验签（`@Sign` 字段参与签名）
- `JwtUtils`：HMAC256 JWT 编解码
- `DataMaskingUtils`、`DefaultDataMasker`：脱敏编码/解码
- `RedissonLockUtils`：`RLock` 加锁执行模板
- `SimpleEventBus`：轻量事件总线
- `AsyncHandler`：异步队列处理器
- `BaseHttpRequestSupport`：HTTP 请求封装（GET/POST JSON/Form/Multipart）

---

## 7. 包结构速览

- `com.levin.commons.service.proxy`：代理扫描注册能力
- `com.levin.commons.plugin`：插件机制
- `com.levin.commons.service.support`：注入、转换、上下文、缓存等支撑能力
- `com.levin.commons.rbac`：RBAC 核心模型与授权逻辑
- `com.levin.commons.dao.domain`：通用实体接口定义
- `com.levin.commons.processor`：注解处理器
- `com.levin.commons.utils`：工具类集合
- `com.levin.commons.ui.annotation` + `com.levin.commons.amis`：UI/AMIS 相关注解和代码生成

---

## 8. 常见问题排查

### Q1：编译时大量 `setXxx/getXxx/log` 找不到？

优先检查：

- JDK 是否过高（建议先切到 JDK 11）
- Lombok 注解处理是否开启
- IDE 与 Maven 使用的 JDK 是否一致

### Q2：引入后某些类运行时报 `ClassNotFound`？

因为本库很多依赖是 `provided`，请在业务工程显式补齐对应依赖。

### Q3：自动配置不想启用怎么办？

通过上文第 5 节列出的 `...=disable` 属性逐项关闭。

---

## 9. 建议落地步骤

1. 先用 JDK11 成功构建本库
2. 在业务工程按需引入本库与运行时依赖
3. 先接入低风险模块（`E_` 常量、日期/枚举转换、工具类）
4. 再引入代理扫描、变量注入、RBAC、插件机制等高耦合模块
