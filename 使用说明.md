# service-support 快速使用说明

完整手册请阅读 [manual.md](manual.md)。本文只保留接入前最常用的信息。

## 1. 项目定位

`service-support` 是一个面向 Spring / Spring Boot 生态的通用支撑库，提供：

- RBAC 权限模型、授权判断、租户/领域/组织范围和数据密级支持
- 多租户、组织树、角色、权限表达式等基础模型
- 代理 Bean 自动注册
- 插件管理器
- 变量解析与字段注入
- Spring MVC 日期、枚举、JSON 参数转换增强
- 字段/方法名常量生成
- 签名、JWT、脱敏、对象只读包装、分布式锁等工具

## 2. 当前版本与环境

当前 `pom.xml`：

```xml
<groupId>com.levin.commons</groupId>
<artifactId>service-support</artifactId>
<version>2.0.0-SNAPSHOT</version>
<maven.compiler.release>17</maven.compiler.release>
```

编译目标为 Java 17。本次已使用 JDK 25.0.2 / Maven 3.9.15，通过显式开启注解处理和测试完成全量验证；构建命令见下节。

## 3. 引入方式

使用当前 Maven 坐标：

```xml
<dependency>
    <groupId>com.levin.commons</groupId>
    <artifactId>service-support</artifactId>
    <version>2.0.0-SNAPSHOT</version>
</dependency>
```

如果通过 JitPack 引入，使用：

```xml
<repositories>
    <repository>
        <id>jitpack.io</id>
        <url>https://www.jitpack.io</url>
    </repository>
</repositories>

<dependency>
    <groupId>com.github.Levin-Li</groupId>
    <artifactId>service-support</artifactId>
    <version>实际发布版本</version>
</dependency>
```

## 4. 构建与测试

项目默认 profile 会跳过测试。只打包：

```bash
mvn clean package
```

运行完整构建与测试时显式开启测试和注解处理：

```bash
mvn -Dmaven.compiler.proc=full -Dmaven.test.skip=false -DskipTests=false clean verify
```

权限模块定向测试：

```bash
mvn -Dmaven.compiler.proc=full -Dmaven.test.skip=false -DskipTests=false \
  -Dtest=DataScopeContractTest,RbacAuthorizeServiceRolePermissionTest clean test
```

## 5. 自动配置

自动配置入口：

- `META-INF/spring.factories`
- `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`

默认自动配置：

- `PluginConfiguration`
- `SpringContextHolder`
- `VariableResolverConfiguration`
- `DefaultSpringMvcDateFormatterConfiguration`
- `DefaultSpringMvcEnumFormatterConfiguration`
- `DefaultSpringMvcJsonDeserializerConfiguration`

关闭方式：

```properties
com.levin.commons.plugin.support.PluginConfiguration=disable
com.levin.commons.service.support.VariableResolverConfiguration=disable
com.levin.commons.service.support.DefaultSpringMvcDateFormatterConfiguration=disable
com.levin.commons.service.support.DefaultSpringMvcEnumFormatterConfiguration=disable
com.levin.commons.service.support.DefaultSpringMvcJsonDeserializerConfiguration=disable
```

## 6. 接入顺序建议

推荐从低耦合能力开始：

1. 工具类和基础领域接口
2. 字段/方法名常量生成
3. Spring MVC 参数转换
4. 变量注入
5. 代理 Bean 注册
6. 插件机制
7. RBAC

RBAC 涉及租户、领域、组织树、角色、权限和数据密级，建议在业务模型明确后接入。

### 6.1 数据范围配置

| 维度 | 允许字段 | 拒绝字段 |
|---|---|---|
| 租户 | `tenantScopeList` | `deniedTenantScopeList` |
| 领域 | `domainScopeList` | `deniedDomainScopeList` |
| 组织 | `orgScopeList` | `deniedOrgScopeList` |

六个字段分别判断：`null` 继承生效角色的对应集合；非 `null` 替代角色配置，`[]` 也算显式配置。空允许集合没有授权，空拒绝集合没有排除。选定来源后，按允许匹配结果减去拒绝匹配结果计算。

例如，允许当前租户的销售部门及下级，排除其中的敏感部门，并清除角色的拒绝配置：

```json
{
  "tenantScopeList": ["_DEFAULT_"],
  "deniedTenantScopeList": [],
  "domainScopeList": ["sales"],
  "deniedDomainScopeList": [],
  "orgScopeList": ["sales-dept|SelfAndAllChild"],
  "deniedOrgScopeList": ["sensitive-dept|SelfAndAllChild"],
  "confidentialDataAccessLevel": 1000
}
```

字段使用具体业务 ID。接口默认的 `Set.of()` 也是显式定义，不能用它表示继承；集合中的 `null` 或空字符串是非法规则，不等于字段未定义。

仅平台用户可以跨租户。`_ALL_` 包含有租户 ID 的租户，`_NONE_` 表示无租户数据；二者分别授权。组织起点 `_NONE_` 表示无组织数据，匹配模式不参与判断，但仍使用 `_NONE_|Self` 这样的存储格式。

业务服务需实现 `loadAllDomainList(boolean)` 和 `loadDomain(Serializable)` 来提供 `RbacDomainInfo` 目录。`loadUserAccessibleDomainList` 批量返回授权且有效的领域，空授权不触发加载。

使用 `canAccessTenant`、`canAccessDomain`、`canAccessOrg` 判断相应范围；领域单点检查也会验证加载对象存在、ID 一致且有效。租户、组织、用户、角色和菜单的非空 `domainId` 现在是上层权限入口的前置门槛，管理员不能绕过；空值只免除自身领域检查。组织与租户都有领域时必须一致。组织判断包含目标租户检查；这些方法不替代资源动作授权和机密级别检查，也不会自动给 DAO 查询加过滤条件。`checkOrgAccessible` 带父节点和根节点管理约束，适用于组织管理校验。

原始加载方法和参数保持不变。菜单扫描后通过 `filterAccessibleMenuList(user, menus)` 返回按用户过滤的独立副本。角色领域授权先初始化，再筛选其他权限贡献；角色对象密级不参与用户密级的反向计算。用户持有角色编码，本租户有效同码定义优先，共享定义回退；分配前调用 `checkRoleAssignment`，由它重新解析实际定义，不能仅校验传入的共享角色对象。

旧 `OrgScope` / `SimpleOrgScope` / `SimpleDataScope` 和旧组织规则 JSON 需要迁移，详见 [迁移说明](manual.md#203-从旧协议升级)。

## 7. 详细文档

见 [manual.md](manual.md)，其中包含：

- 包结构和模块说明
- 代理 Bean 示例
- 插件机制
- 变量注入
- 对象只读包装
- RBAC 最小接入流程
- `DataScope` 六个允许/拒绝集合与 `DataScope.OrgScope` 字符串规则
- 组织树装配和性能建议
- 常见问题排查
